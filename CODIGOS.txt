npm install -g @nestjs/cli //instalar

npm i class-validator class-transformer  //baixar biblioeteca de validacao de transformacao
npm i @nestjs/mapped-types //PartialType de Mapped-Types para a validação de campos em UpdateRecadoDto
                            //facilita impede repeticao

npm i @nestjs/typeorm typeorm pg  //typeorm com postgres

npm i @nestjs/config  //intepretar .env 
npm i @hapi/joi // facilita para validar e passar .env
npm i -D @types/hapi__joi  //tipagem

npm i bcryptjs // fazer hashs criptografados
npm i -D @types/bcryptjs //tipagem

npm i @nestjs/jwt //JWT

npm i -D @types/multer //upload de arquivos

npm install --save @nestjs/serve-static

nest new NOME_PASTA // iniciar projeto
nest new conceitos_nest --skip-git // iniciar sem git

npm run start // rodar
npm run start:dev //rodar em watch mode

nest g co SUA_PASTA --no-spec  //criar controllers sem testes

nest g mo NOME // criar modulo

nest g s SUA_PASTA --no-spec // criar service e atualizar

nest g cl PASTA/dto/create-recado.dto --no-spec  // Data Transfer Object

nest g res USUARIOS // gera recursos crud automaticamente

g = generate 
mo = module, co = controller, s = service

npm run lint

---------
teorico:
---------

https codes
respostas informativas 100 - 199
bem sucedidas 200 - 299
mensagens de redirecionamento 300 -399
erro cliente 400 - 499
erro do servidor 500 - 599

NESTJS interceptor -
@Injectable() -> injeção de dependencias
passa oque tem dentro de uma classe de forma automatica

// service.ts
import { Injectable } from '@nestjs/common';

@Injectable()
export class MyService {
  getHello(): string {
    return 'Olá, mundo!';
  }
}

// controller.ts
import { Controller, Get } from '@nestjs/common';
import { MyService } from './service';

@Controller()
export class MyController {
  constructor(private readonly myService: MyService) {}

  @Get()
  getHello(): string {
    return this.myService.getHello();
  }
}

middlewares: é uma função genérico muito perto do express/servidor com a requisicao e resposta do cliente
// Cliente (navegador) -> (Servidor) -> Middleware recebe (request, response)
// -> NestJs (Guards, interceptors, pipes, filters)



----------------
parametros personalizados
em common: params
//user-param interessante

---------------------
dependencia circular é quando um modulo importa um ao outro e usa forwardRef(()=> MODULO) para arrumar

-------------------
mock: 
testes, fingi uma classe para escrever testes

-------------------
classes abstratas e interfaces com provide e useClass para padrões de projeto
padrões de projeto otavio miranda curso - ver

interfaces ou  classes abstratas para fazer um modelo 

---------
JWT é um token
------------

testes

npm run test //procura e roda todos os .spec 

npm run test:watch //para ficar sempre rodando o observando os teste. precisa de git
npm run test --watchAll //nao precisa de git

npm run test:cov // 

npm run test:e2e //end to end
